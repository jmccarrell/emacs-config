I want to work on: AI / LLM integration

This is a big goal, and needs a careful, step by step plan that wont overwhelm Jeff with changes.

The first task is to identify useful smaller sub-goals, such as:

1. survey the state of emacs packages that support local LLM usage talking to a locally deployed model
1. attempt to understand the patterns that allow emacs users to switch between local and cloud LLM as needed
1. understand how code completion can use LSP vs an LLM

Or other sub goals you might infer from reading the emacs source code in our reference emacs configs.
Or search the web for blogs on emacs using LLMs.
Also remember that we can find ground truth for new emacs packages we might install in an investigation via the just recipes in a worktree.

Spin up a worktree and start planning.
