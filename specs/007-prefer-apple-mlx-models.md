Read this blog post announcing support for faster local models running on Apple Silicon and make a recommendation for how I could try this out in my local ollama.

I believe ollama --verbose will report tokens per second.

I want to compare the throughput in tokens per second of my existing gemma4 model on a simple prompt eg: "Hi" to an equivalent MLX enabled model.

Make recommendations.
